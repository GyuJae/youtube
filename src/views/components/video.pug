mixin video(video)
    div 
        h4 
            a(href=`/videos/${video.id}`)=video.title    
        ul
            li #{video.meta.rating}/5
            li Posted #{video.createdAt}.
            li Posted #{video.description}.
            li #{video.meta.views} views