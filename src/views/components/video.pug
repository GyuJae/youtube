mixin video(video)
    div.video_container
        a(href=`/videos/${video.id}`)
            div.video
            div.title #{video.title}
            div.views #{video.meta.views} views
            